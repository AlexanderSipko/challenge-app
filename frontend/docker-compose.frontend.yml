services:
  frontend:
    build: 
      context: .  # ← Текущая папка frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://localhost:3001
    command: npm start